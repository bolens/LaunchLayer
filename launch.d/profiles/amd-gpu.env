# AMD GPU Global Optimizations
NVIDIA_POWER_MODE=0
GPU_POWER_CHECK=0

# Prefer RADV over AMDVLK
AMD_VULKAN_ICD=RADV

# Multi-threaded OpenGL in Mesa
mesa_glthread=true

# Optional RADV experiments (uncomment if desired; do not force globally):
# RADV_PERFTEST=sam
