# Follow `source` directives so lib/*.sh modules are analyzed as one program.
external-sources=true
# Resolve `# shellcheck source=platform/foo.sh` in lib/load-modules.sh.
source-path=lib
# Nameref out-parameters (false positives when analyzing launchlayer with -a).
disable=SC2178
